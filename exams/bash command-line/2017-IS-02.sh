#!/bin/bash

# Зад. 4 2017-IN-02
# Напишете серия от команди, които изтриват:
# а) всички файлове в текущата директория и нейните под директории, които са с нулева дължина.
# б) 5-е най-големи файла в home директорията на текущия потребител и нейните под директории.


find . -type f -size 0 -exec rm {} \;
find . -type f -exec ls -l {} \; | sort -t ' ' -k 5 -n -r | head -n 5
